version: '3'

services:
  app:
    image: littlesis/littlesis:1.1.3
    stdin_open: true
    tty: true
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./rails:/littlesis
      - ./docker/tmp:/littlesis/tmp
    depends_on:
      - redis
      - mysql
    environment:
      - LITTLESIS_BIND=tcp://0.0.0.0:8080

  redis:
    image: redis:6

  mysql:
    image: mariadb:10.5
    volumes:
      - ./mysql-data:/var/lib/mysql
    ports:
      - '127.0.0.1:3306:3306'
    environment:
      - MYSQL_ROOT_PASSWORD=root

  nginx:
    image: nginx:latest
    ports:
       - "127.0.0.1:8081:80"
    volumes:
      - ./rails:/littlesis
      - ./static:/var/www/static
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./docker/log:/var/log/nginx
    depends_on:
      - app
